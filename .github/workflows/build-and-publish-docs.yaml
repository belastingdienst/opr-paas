name: Build and publish documentation
on:
  workflow_dispatch:
  release:
    types:
      - published
env:
  PYTHON_VERSION: 3.x

permissions:
  contents: write
  id-token: write
  pages: write

jobs:
  build:
    name: Build documentation
    runs-on: ubuntu-latest
    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go for generating API doc
        uses: actions/setup-go@v5

      - name: Generate API document
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          go install github.com/elastic/crd-ref-docs@6d9cbb2645274941d10941cc988a49e873562fba
          crd-ref-docs --config=./crd-ref-docs-config.yml --source-path=./api --renderer=markdown --output-path=./docs/development-guide/00_api.md
        working-directory: ${{ github.workspace }}

      - name: Set up Python runtime
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
          cache-dependency-path: |
            requirements.txt

      - name: Set up build cache
        uses: actions/cache/restore@v4
        with:
          key: mkdocs-material-${{ hashfiles('.cache/**') }}
          path: .cache
          restore-keys: |
            mkdocs-material-

      - name: Install Python dependencies
        run: |
          pip install --no-cache-dir mkdocs-material \
                                     mkdocs-literate-nav \
                                     mkdocs-redirects \
                                     mkdocs-material[recommended] \
                                     mkdocs-material[imaging] \
                                     mike

      - name: Configure Git user
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

      - name: Extract short tag
        id: extract_short_tag
        run: |
          TAG="${{ github.event.release.tag_name }}"
          # Strip leading "v" if present, then drop everything after the second dot
          SHORT_TAG="$(echo "$TAG" | sed 's/^v//; s/\([0-9]*\.[0-9]*\).*/\1/')"
          echo "SHORT_TAG=$SHORT_TAG" >> $GITHUB_OUTPUT

      - name: Deploy with mike 🚀
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          SHORT_TAG=${{ steps.extract_short_tag.outputs.SHORT_TAG }}
          git pull -r
          mike deploy --update-aliases --push "$SHORT_TAG" latest
          mike --version
          
      - name: Save build cache
        uses: actions/cache/save@v4
        with:
          key: mkdocs-material-${{ hashfiles('.cache/**') }}
          path: .cache
